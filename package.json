{
  "name": "nicosia-chaos-league",
  "version": "12.0.0",
  "private": true,
  "description": "Nicosia Chaos League - Phaser + Socket.IO multiplayer prototype.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm --workspace server run dev\" \"npm --workspace client run dev\"",
    "build": "npm --workspace client run build",
    "start": "npm --workspace server run start",
    "deploy:check": "npm install && npm run build && npm start",
    "clean": "node -e \"require('fs').rmSync('client/dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
